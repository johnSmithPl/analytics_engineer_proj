name: 'analytics_engineer_proj'
version: '1.0.0'
config-version: 2

profile: 'analytics_engineer_proj'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  analytics_engineer_proj:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
